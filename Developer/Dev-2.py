import netmiko
import netmiko from ConnectionHandler

dev = {
         username = '$username'
	 password = '$password'
	}

Connection = ConnectionHandler.connect(dev)
print  "The connecion happend  " Connection
Adding new Line
